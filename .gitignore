# dependencies
node_modules/

# next.js
.next/
out/
next-env.d.ts

# vite
dist/
.vite/

# env — never commit real keys; .env.example documents every variable
.env
.env.*
!.env.example

# builds & misc
*.tsbuildinfo
*.log
.DS_Store

# playtest artifacts (screenshots, recordings) — proof lives in the demo, not git
playtest-artifacts/
local-games/

# ad-scenarios toolkit output (rejouable variant JSON + recorded .webm clips)
generated-variants/

# throwaway verification scripts — means-to-verify, never committed (AGENTS.md)
.scratch/

# local-only files (reference videos, private notes) — never pushed
.private/
